= form_tag follows_path, :method => :post, :remote => true do
  = hidden_field_tag :id, @photo.venue.id
  = hidden_field_tag :photo_id, @photo.id
  = submit_tag "Favorite venue", :class => 'btn large'