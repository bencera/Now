= form_tag root_url + "usefuls/#{@photo.id}", :method => :delete, :remote => true do
  = hidden_field_tag :id, @photo.id
  = submit_tag "useful", :class => 'btn success', :id => 'useful', :style => "width: 50px; height: 15px; padding: 0; font-size: 10px; "