<%= "http://www.ubimachine.com/events/trending/confirm/#{@new_event.id}" %>
<% @new_event.other_descriptions.each do |od| %>
	<%= "#{FacebookUser.where(:facebook_id => od[0]).first.fb_details['name']}: #{od[1]} : #{od[2]} @ #{@new_event.venue.name} " %>
<% end %>