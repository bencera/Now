<% if @photo.blank? %>
$("#venue" + "<%= "#{@venue.id}" %>").html("<%= escape_javascript(render('partials/unfollow')) %>")
<% else %>
$("#show_venue" + "<%= "#{@photo.venue.id}" %>").html("<%= escape_javascript(render('partials/unfollow_show')) %>")
$("#venue" + "<%= "#{@photo.venue.id}" %>").html("<%= escape_javascript(render('partials/unfollow')) %>")
<% end %>
$(".followers").html('<%= "#{@venue.users.count} followers" %>')