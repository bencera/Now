collection @comments
attributes :user_id, :user_full_name, :user_photo, :message, :timestamp
#node(:user_id) do |u|
#  u[:user_id]
#end

#node(:user_full_name) do |u|
#  u[:user_full_name]
#end

#node(:user_photo) do |u|
#  u[:user_photo]
#end

#node(:message) do |u|
#  u[:message]
#end

#node(:timestamp) do |u|
#  u[:timestamp]
#end
