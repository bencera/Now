$(".useful_btn#photo" + "<%= "#{@photo.id.to_s}" %>").html("<%= escape_javascript(render('partials/unuseful')) %>")
$(".card#photo_card_" + "<%= "#{@photo.id.to_s}" %>" + " .new_caption").hide();
$(".card#photo_card_" + "<%= "#{@photo.id.to_s}" %>" + " .comment").text("<%= "#{@new_caption}" %>");
$(".card#photo_card_" + "<%= "#{@photo.id.to_s}" %>" + " .comment").fadeIn();
$('.card').wookmark({offset: 10, container: $('.container_photos'), itemWidth: 240});
