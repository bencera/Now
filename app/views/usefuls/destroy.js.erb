$(".useful_btn#photo" + "<%= "#{@photo.id.to_s}" %>").html("<%= escape_javascript(render('partials/useful')) %>")
$(".useful_count#photo" + "<%= "#{@photo.id.to_s}" %>").html('<%= pluralize(photo.usefuls.count, 'person') unless photo.usefuls.count == 0 %><%= "found this photo useful" unless photo.usefuls.count == 0 %>')
$('.card').wookmark({offset: 10, container: $('.container_photos'), itemWidth: 240});