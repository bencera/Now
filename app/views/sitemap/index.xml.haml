- base_url = "http://#{request.host_with_port}"
!!! XML
%urlset{:xmlns => "http://www.sitemaps.org/schemas/sitemap/0.9"}
  - for event in @events
    %url
      %loc #{base_url}#{event.shortid}
      %lastmod=event.endtime